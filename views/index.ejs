<!DOCTYPE html>
<html>
    <% include common/head %>
    </head>
    <body class="pg-blog-index">
        <% include common/header %>
        <div class="body cf">
            <div class="content">
                <div class="main blog-list">
                    <% blogs.forEach(function (blog) { %>
                    <% var link = '/blog?bname=' + blog.bid; %>
                    <div class="blog">
                        <div class="blog__cont">
                            <h2><a href="<%= link %>"><%- blog.title %></a></h2>
                            <p class="text"><%- blog.summary %></p>
                        </div>
                        <div class="blog__info">
                            <a href="<%= link %>" class="time"><span><%= blog.day %><br /><%= blog.year %></span></a>
                            <a href="<%= link %>" class="plus"><span></span></a>
                        </div>
                        <div class="blog__tag">
                            <% for(var i = 0; i < blog.tags.length; i++) {%>
                                <a href="/history?tag=<%= blog.tags[i]%>">
                                    <%= blog.tags[i] %>
                                </a>
                            <%}%>
                        </div>
                    </div>
                    <% }) %>
                </div>
            </div>
            <aside>
                <div class="about">
                    <a class="link" href="/">
                        <img src="<%= avatar %>" />
                    </a>
                </div>
                <iframe width="100%" height="550" class="share_self"  frameborder="0" scrolling="no" src="http://widget.weibo.com/weiboshow/index.php?language=&width=0&height=550&fansRow=1&ptype=1&speed=0&skin=1&isTitle=1&noborder=1&isWeibo=1&isFans=1&uid=2448299880&verifier=ded7228b&dpc=1"></iframe>
            </aside>
        </div>
        <% include common/footer %>
    </body>
</html>
